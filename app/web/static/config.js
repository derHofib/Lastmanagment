// Standard: echter Betrieb (Backend/FastAPI vorhanden).
// Der GitHub-Pages-Workflow überschreibt diese Datei mit "true", damit die
// Oberfläche dort ohne Backend im Demo-Modus (simulierte API) läuft.
window.LM_DEMO = false;
