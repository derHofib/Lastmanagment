# Laufzeitabhängigkeiten für das Lastmanagement
# Getestet auf Raspberry Pi OS (ARM) mit Python 3.11+
fastapi>=0.110
uvicorn[standard]>=0.29
pymodbus>=3.6
SQLAlchemy>=2.0
pydantic>=2.6
PyYAML>=6.0

# Nur für Tests / Entwicklung
httpx>=0.27
pytest>=8.0
pytest-asyncio>=0.23
